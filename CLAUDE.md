# [Claude Code 작업 지시서] 외자부품 Invoice 엑셀 자동 변환 (Foreign Parts DB)

- 기획자: 권도연
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 수강생 프로젝트 기획서 16번
- 프로젝트 유형: 문서 판독(전자 PDF · 스캔본 · 엑셀) → 검증·수정 → ERP 업로드용 엑셀
- 대체 대상: **해외 공급사 Invoice 를 사람이 눈으로 보고 손으로 옮겨 치던 일**

> ⚠ **이 문서는 기획서 원문이 아니라 저장소에서 되짚어 쓴 것이다.**
> 구현이 먼저 있었고 지시서가 없었다. §1~§5 는 **README·첫 커밋 메시지·
> 코드·테스트에서 실제로 확인한 것만** 옮겼고, **부록 A(기획서 원문)는
> 비워 두었다.** 지어내면 다음 사람이 그것을 기획자의 말로 믿는다 —
> 이 도구가 못 읽은 값을 지어내지 않는 것과 같은 이유다.
>
> **다만 이 저장소는 13·14·15번과 사정이 조금 다르다.** 원문의 한 조각이
> README 에 인용으로 남아 있고(§6), 그 조각이 **아직 받지 못한 자료**를
> 가리킨다. 부록 A 보다 그쪽이 먼저다.

---

## 0. Claude Code에게 — 작업 방식

1. 이 도구의 출력은 **ERP 에 올라간다.** 손으로 칠 때는 한 건이 틀리지만,
   **잘못 읽은 규칙은 백 줄을 한꺼번에** 틀리게 만든다. 그래서 이 저장소의
   규칙은 대부분 "틀렸을 때 어떻게 드러나느냐"에 대한 것이다.
2. 판정하는 코드는 `js/invoice.js` 에 **순수 함수**로 모으고
   `test/logic.test.js`(91개)로 고정한다. 화면(`js/app.js`)은 판정하지 않는다.
   `js/read.js` 는 파일에서 글자를 꺼내는 일만 한다.
3. **양식(열 이름·열 순서)에 기대는 규칙을 새로 만들지 않는다.** §2 가 이
   저장소의 중심 생각이고, 새 공급사가 와도 코드를 고치지 않아도 되는 이유다.
4. **바깥을 부르지 않는다.** 폐쇄망에서 돌아야 하고 **Invoice 파일이 서버로
   나가면 안 된다.** 필요한 라이브러리는 전부 `lib/` 에 넣는다(§4).
5. **사내 실데이터를 저장소에 넣지 않는다.** 공급사명·품번·금액이 든 실제
   Invoice 는 커밋하지 않는다. 테스트는 지어낸 표를 쓴다.

---

## 1. 한 줄 요약

해외 공급사에서 오는 Invoice(전자 PDF · 스캔본 · 사진 · 엑셀)에서 부품 명세를
뽑아 **검산으로 걸러 내고 사람이 고친 뒤** ERP 업로드용 엑셀 한 장으로 합치는 도구.

## 2. 중심 생각 — 계산이 맞는지로 읽은 것이 맞는지 판단한다

공급사마다 열 순서도 이름도 다르다.

```
A사: Part No | Description | Qty  | Unit Price | Amount
B사: Pos     | Artikel     | Preis| Menge      | Betrag     ← 단가가 수량보다 앞
C사: 번호    | 품명        | 수량 | 단가       | 금액
```

열 이름으로 맞히려 들면 **못 보던 양식에서 통째로 틀린다.**
그런데 Invoice 에는 반드시 성립하는 식이 하나 있다.

> **수량 × 단가 = 금액**

그래서 한 줄에서 숫자를 전부 뽑아(`numberTokens`) **어느 조합이 이 식을
만족하는지** 찾는다(`fitLine`). 맞아떨어지면 그 조합이 수량·단가·금액이다.
**열 순서를 몰라도 된다.**

덤으로 둘이 같이 풀린다.

- **유럽식 소수점** — `1.234,56` 은 독일에서 1234.56 이다. 미국식으로 읽으면
  1.234 가 되어 **천 배가 틀리는데** 화면에는 그냥 숫자로 보인다.
  두 방식으로 다 읽어 보고 계산이 맞는 쪽을 고른다(`readNumber`).
- **문자인식 오류** — 스캔본에서 8 을 3 으로 읽으면 계산이 어긋나 그 줄이 드러난다.

### 곱셈만으로는 못 가리는 것 — 표 전체를 본다

`2.480,00 × 2 = 4.960,00` 은 **수량 2·단가 2480** 으로도 **수량 2480·단가 2**
로도 아귀가 맞는다. 검산으로는 절대 못 가린다. 실제로 개발 중에 뒤바뀌었다.

같은 표의 다른 줄(`1.234,56 × 10 = 12.345,60`)은 수량이 정수여야 한다는
조건에서 자리가 확정된다. **표는 열이 일정하므로 그 자리를 표 전체에 적용**하면
애매한 줄도 풀린다(`parseLines`). 자리는 **뒤에서부터** 센다 —
품번에 숫자가 섞여 있어(`DIN-912-M8`) 앞에서 세면 어긋난다.

## 3. 이중 엔진 — 어느 쪽으로 읽었는지 반드시 남긴다

| 입력 | 어떻게 | 정확도 |
|---|---|---|
| 전자 PDF | 글자를 그대로 꺼낸다 | 높음 |
| 스캔본 PDF | 2배 해상도로 그린 뒤 문자인식 | **낮음 — 검산 필수** |
| 사진(JPG·PNG) | 문자인식 | **낮음** |
| 엑셀 · CSV | 표를 그대로 | 높음 |

쪽당 글자 수가 60자 미만이면 스캔본으로 **자동 판단**하고, 화면에서 강제할 수 있다.

**어느 엔진으로 읽었는지 화면과 엑셀 양쪽에 남긴다.** 값의 신뢰도가 다른데
안 적으면 사람이 어디를 더 봐야 할지 모른다.

## 4. 폐쇄망에서 돌아간다

바깥을 부르는 곳이 **한 군데도 없다. Invoice 파일이 서버로 나가지 않는다.**

| 필요한 것 | 어떻게 |
|---|---|
| PDF 읽기 | `lib/pdf.min.mjs` + `lib/pdf.worker.min.mjs` + `lib/cmaps/` |
| 문자인식 | `lib/tesseract.min.js` + `lib/worker.min.js` + `lib/core/` + `lib/tessdata/` |
| 엑셀 | `lib/xlsx.full.min.js` |

`lib/` 이 15MB 로 크다. 문자인식 엔진과 언어 데이터 때문인데
**빼면 스캔본을 아예 못 읽으므로 줄일 수 없다.**

⚠ `lib/` 은 `js/read.js` 의 `libUrl()` 로 **늦게** 불린다. HTML 에 적혀 있지
않아 눈으로는 안 보이고, 하나가 빠져도 **단위 테스트 91개는 전부 통과한다**
(순수 함수는 `lib` 를 안 쓴다). CI 의 「동봉 라이브러리가 다 있는가」가 이 자리를 센다.

---

## 5. 반드시 규칙으로 막을 것 (테스트로 고정)

`test/logic.test.js` 91개가 지킨다. ★ 는 틀리면 **잘못된 값이 ERP 로 올라가는** 것.

| # | 규칙 | 어디서 |
|---|---|---|
| 1 | ★ 열 이름이 아니라 **검산으로** 수량·단가·금액을 정한다 | `Invoice.fitLine` |
| 2 | ★ 천단위는 **반드시 세 자리씩** 끊는다 — 안 그러면 `12.50`→1250 이 되어 검산이 무력해진다 | `Invoice.readNumber` |
| 3 | ★ 한 줄로 안 갈리면 **표 전체의 자리**를 쓴다. 자리는 뒤에서부터 센다 | `Invoice.parseLines` |
| 4 | ★ 계산이 안 맞는 줄을 **버리지 않는다.** 짐작해 넣고 「짐작」 표시 | `Invoice.checkRow` |
| 5 | `03/04/2026` 을 단정하지 않는다 — **애매하다고 표시만** 한다 | `Invoice.readDate` |
| 6 | 라벨은 **엄격한 것부터** 찾는다 — 느슨하게 돌면 `COMMERCIAL INVOICE` 제목이 Invoice No 가 된다 | `Invoice.parseHeader` |
| 7 | 합계·운임 줄을 부품으로 섞지 않고 **몇 줄 건너뛰었는지 알린다** | `Invoice.parseLines` |
| 8 | 주소·전화번호를 부품으로 보지 않는다(숫자가 셋이라 걸린다) | 같음 |
| 9 | 엑셀은 **부품 한 줄마다 헤더를 통째로** 붙인다 | `Invoice.flatten` |
| 10 | `읽은줄`(원문)을 남긴다 — **틀렸을 때 어디서 왔는지 봐야 한다** | 같음 |

### 검증·수정 화면이 이 도구에서 가장 중요하다

> **자동으로 읽은 값을 안 보고 그대로 ERP 에 올리지 않는다.**

- 칸을 눌러 바로 고친다. **수량·단가를 고치면 금액이 자동 재계산**된다.
- **금액을 직접 고치면 그대로 둔다** — 원본 금액이 맞고 단가가 반올림된 경우가 있다.
- 헤더 열은 기본으로 접혀 있다. 켜 두면 정작 봐야 할 **검산 열이 화면 밖으로 밀린다.**
- 가장 빠른 확인법: **문서 카드의 합계를 원본 Invoice 의 Sub Total 과 맞춰 본다.**

---

## 6. 먼저 받아야 할 것 — 형광펜 표시된 실물 샘플

README 에 기획서 원문의 한 조각이 인용으로 남아 있다.

> "첨부 PDF에 형광펜으로 추출 대상 필드를 표시해 둠"

**그 파일을 받지 못했다.** 지금은 양식에 기대지 않는 방식이라 대부분의 Invoice 를
읽지만, **실제 공급사 양식으로 맞춰 본 적이 없다.** 특히 이런 것이 남는다.

- **여러 줄에 걸친 품명** — 둘째 줄이 따로 떨어져 나온다
- **수량·단가·금액이 한 줄에 흩어진 양식** — 지금은 한 줄 안에 있어야 찾는다
- **공급사별 파싱 규칙(템플릿) 저장** — 실물을 보고 나서 만드는 편이 맞다

샘플을 받으면 곧바로 맞춘다. 그전까지 확인은 **합계 대조**로 한다.

## 7. 산출물

- 빌드 없이 도는 순수 HTML/CSS/JS (GitHub Pages 배포) — 페이지 두 장(`index.html`·`guide.html`)
- `test/logic.test.js` 91개
- `test/smoke.browser.js` 29개 — 브라우저에 실제로 띄워 예시 세 문서를 지나간다
- 라이브러리 동봉(`lib/`, 15MB) — 폐쇄망 전제
- `README.md`, `docs/개발일지.md`

---

## 부록 A. 기획서 원문

> **비어 있음.**
>
> 이 저장소에는 기획자(권도연)의 기획서 원문이 남아 있지 않다.
> 다른 프로젝트의 작업 지시서는 이 자리에 원문을 그대로 싣고 있고,
> 그 원문이 최종 기준 역할을 한다.
>
> 남아 있는 것은 **한 조각의 직접 인용**뿐이다 —
> "첨부 PDF에 형광펜으로 추출 대상 필드를 표시해 둠"(§6).
> 이 문장은 원문에서 온 것이 분명하고, **아직 받지 못한 자료**를 가리킨다.
>
> **채우는 방법**: 원문을 받아 이 아래에 인용(`>`)으로 그대로 붙인다.
> 붙인 뒤 §1~§5 가 원문과 어긋나는 곳이 없는지 확인한다 —
> 지금 적힌 것은 구현에서 되짚은 것이라, **기획자가 요구했지만 구현되지 않은
> 항목은 여기 없을 수 있다.** 특히 "추출 대상 필드"의 목록이 그렇다.
>
> 요약하거나 다시 쓰지 말 것. 원문 그대로가 기준이다.
